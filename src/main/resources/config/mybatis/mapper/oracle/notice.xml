<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.mapper.oracle.notice">
	<insert id="insertNotice" parameterType="NoticeVO">
		<selectKey keyProperty="NOTI_NO" resultType="int" order="BEFORE">
			select notice_seq.nextval from dual
		</selectKey>
		
		insert into notice(NOTI_NO, NOTI_TITLE, MEMNO, NOTI_CONTENT, 
			NOTI_FILENAME, NOTI_FILESIZE, NOTI_ORFILENAME, 
			NOTI_CATEGORY, NOTI_MAIN)
		values(#{NOTI_NO}, #{NOTI_TITLE}, #{MEMNO}, #{NOTI_CONTENT}, 
			#{NOTI_FILENAME}, #{NOTI_FILESIZE}, #{NOTI_ORFILENAME}, 
			#{NOTI_CATEGORY}, #{NOTI_MAIN})
	</insert>
	
	<select id="selectAll" resultType="NoticeVO" parameterType="searchVo">
		select *
		from
		(
		    select rownum as RNUM, A.*
		    from(
		        select NOTI_NO, NOTI_TITLE, NOTI_REGDATE,
			    NOTI_READCOUNT, NOTI_CONTENT,
			    (sysdate-NOTI_REGDATE)*24 as newImgTerm 
			    from notice
		       <!--  <if test="searchKeyword!=null and searchKeyword !=''">
		        	where ${searchCondition} like '%' 
		        			|| #{searchKeyword} || '%'
		        </if> -->
		        order by NOTI_NO desc
		    )A    
		)
		<![CDATA[ 
		where RNUM>#{firstRecordIndex}
		  and RNUM<=#{firstRecordIndex }+ #{recordCountPerPage}]]>
	</select>
	
	<select id="selectTotalRecord" parameterType="searchVo"
		resultType="int">
		select count(*) from notice
        <if test="searchKeyword!=null and searchKeyword !=''">
        	where ${searchCondition} like '%' 
        			|| #{searchKeyword} || '%'
        </if>
	</select>
	
	<update id="updateReadCount" parameterType="int">
		update notice 
		set NOTI_READCOUNT=NOTI_READCOUNT+1
		where NOTI_NO=#{NOTI_NO}
	</update>
</mapper>









