<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.mapper.oracle.member">
	<select id="selectPwd" parameterType="string"  resultType="string">
		select pwd from log_mem
		where memno = #{memNo} and firedate is null 
		</select>

	<select id="selectMember" parameterType="string" resultType="MemberVO">
		select * from log_mem
		where memno = #{memNo} 
	</select>

	<select id="logCheck" parameterType="MemberVO_log">
		select * from log_mem
		where memno = #{memNo} and pwd = #{pwd}
	</select>
	
<<<<<<< HEAD
	<insert id="admin_regist_member" parameterType="memberVO">
		insert into member(memno, name, pwd, hiredate, deptCode, posCode, salary)
		values(#{memNo}, #{name}, #{pwd}, #{hiredate}, #{deptCode}, #{posCode}, #{salary} )
			
=======
	<insert id="registerMulti" parameterType="memberVO">
		insert into member(memNo, pwd, name, HIREDATE ,deptcode, POSCODE , salary)
		values(#{memNo}, #{pwd} ,#{name},#{hiredate}, #{deptCode}, #{posCode}, #{salary})
		
>>>>>>> parent of d253f43... sisi
	</insert>
	
	<select id="searchAllmember" resultType="memberVo">
		select * from member
	</select>
</mapper>