<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.mapper.oracle.boardComment">
	<insert id="commentInsert" parameterType="BoardCommentVO" >
        <selectKey keyProperty="no" resultType="int" order="BEFORE">
			select boardComment_seq.nextval from dual
		</selectKey>
        INSERT INTO
        commentBoard (no, boardNo, memNo, nickname, content, regdate)
        VALUES(#{no}, #{boardNo}, #{memNo},#{nickname},#{content}, regdate) 
    </insert>
    
    
    <select id="commentList" parameterType="List">
    	select no, boardNo, memNo, nickname, content, regdate
    	from boardComment 
    	where boardNo=#{boardNo}
    	order by no
    </select> 
    
    <update id="commentUpdate" parameterType="BoardCommentVO">
    	update commentUpdate
    	set content=#{content}
    	where no=#{no}
    </update>
    
    <delete id="commentDelete" parameterType="int">
    	delete from commentDelete
    	where no=#{no}
    </delete>
    
    
	<!-- <select id="commentCount" resultType="int">
        SELECT
            COUNT(*)
        FROM boardComment
    </select>
    
    <select id="commentList" resultType="BoardCommentVO">
        SELECT
            *
        FROM COMMENT
    </select>

    <update id="commentUpdate" parameterType="BoardCommentVO">
        UPDATE COMMENT
            SET
            CONTENT = #{content}
        WHERE CNO = #{cno}
    </update>
    
    <delete id="commentDelete" parameterType="int">
        DELETE FROM COMMENT WHERE CNO = #{cno}
    </delete>
 -->
</mapper>









