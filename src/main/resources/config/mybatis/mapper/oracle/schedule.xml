<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.mapper.oracle.schedule">
	<insert id="insertSchedule" parameterType="ScheduleVo">
		<selectKey keyProperty="scheduleNo" resultType="int" order="BEFORE">
			select schedule_seq.nextval from dual
		</selectKey>
		insert into SCHEDULE(title,content,scheduleStart,scheduleEnd,scheduleNo,empNo)
		values(#{title},#{content},#{scheduleStart},#{scheduleEnd},#{scheduleNo},#{empNo})
	</insert>
	
</mapper>









