<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.mapper.oracle.schedule">
	<insert id="insertSchedule" parameterType="ScheduleVo">
		<selectKey keyProperty="scheduleNo" resultType="int" order="BEFORE">
			select schedule_seq.nextval from dual
		</selectKey>
		insert into SCHEDULE(title,content,scheduleStart,scheduleEnd,scheduleNo,memNo,type)
		values(#{title},#{content},#{scheduleStart},#{scheduleEnd},#{scheduleNo},#{memNo},#{type})
	</insert>
	
	<select id="selectSchedule" parameterType="string" resultType="ScheduleVo">
		select * from schedule where memNo=#{memNo}
	</select>
	
</mapper>









