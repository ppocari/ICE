<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.mapper.oracle.notice">
	<insert id="insertNotice" parameterType="NoticeVO">
		<selectKey keyProperty="no" resultType="int" order="BEFORE">
			select notice_seq.nextval from dual
		</selectKey>
		
		insert into notice(NOTI_NO, NOTI_TITLE, memNo, NOTI_CONTENT, NOTI_REGDATE, NOTI_FILENAME, NOTI_ORFILENAME, NOTI_DOWNCOUNT,
			NOTI_READCOUNT, NOTI_CATEGORY, NOTI_MAIN, newImgTerm)
		values(#{NOTI_NO}, #{NOTI_TITLE}, #{memNo}, #{NOTI_CONTENT}, #{NOTI_REGDATE}, 
			#{NOTI_FILENAME}, #{NOTI_ORFILENAME}, #{NOTI_DOWNCOUNT},
			#{NOTI_READCOUNT}, #{NOTI_CATEGORY}, #{NOTI_MAIN}, #{newImgTerm})
	</insert>
</mapper>









