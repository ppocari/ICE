<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.mapper.oracle.comcard">
	<select id="selectUnUseComcard" parameterType="SearchYearMonthVO" resultType="ComcardVO">
		select * from comcard_mem
		where acccode is null 
		<![CDATA[ 
		and usedate >= #{begin_ym} 
		and usedate < #{end_ym}
		]]>
		  
	</select>
	
	<select id="selectNoComcard" parameterType="int" resultType="ComcardVO">
		select * from comcard_mem
		where acccode is null
		  and no = #{comcard_no}
	</select>
	
	<select id="selectAllComcard" resultType="ComcardVO">
		select * from comcard_mem
	</select>
</mapper>